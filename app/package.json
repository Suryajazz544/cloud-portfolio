{
  "name": "cloud-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "A full-stack Node.js portfolio deployed on a highly available AWS architecture.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  }
}
